# Chapter 12: Coverage

Run the test suite:

```bash
$ uv run pytest -q
```

Run a subset of tests (step-by-step demos used in the chapter):

```bash
$ uv run pytest -q -k step1
$ uv run pytest -q -k step2
$ uv run pytest -q -k step3
```

